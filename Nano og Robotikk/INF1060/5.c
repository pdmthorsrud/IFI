#include <stdio.h>

boolean ishex(char current_char){
  return (current_char >= '0' && current_char <= '9' && current_char>='a' && current_char<='f')
}

int main(){

boolean hello=1;
long a = 10000

  printf("Is c a hex = %d", ishex("c"));

}










































